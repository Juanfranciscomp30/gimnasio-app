// Zona ADMIN: gestión de usuarios.
// TODO: listar usuarios, editar tarifa (1/2/3 días semana), y
// añadir un usuario MANUALMENTE (createdByAdmin = true).
export default function UsuariosAdminPage() {
  return <div>TODO: Gestión de usuarios + alta manual (solo admin)</div>;
}

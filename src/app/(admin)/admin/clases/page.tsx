// Zona ADMIN: ver qué personas hay en cada clase/día.
// TODO: listar ClassSession por fecha con sus Booking (CONFIRMED).
export default function ClasesAdminPage() {
  return <div>TODO: Ver aforo y asistentes por clase (solo admin)</div>;
}
